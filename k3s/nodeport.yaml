apiVersion: v1
kind: Service
metadata:
  name: mzhtml-service-nodeport
spec:
  selector:
    app: mzhtml
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000
  type: NodePort

